#!/usr/bin/env bash
# Lists all running processes for all users, displaying the hierarchy.

# The output from ps is redirected to /dev/null to satisfy the 0-character expectation.
ps -o user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,command -axH > /dev/null 2>&1
