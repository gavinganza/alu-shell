#!/usr/bin/env bash
# Lists all running processes for all users, displaying the hierarchy.

# The command 'ps -efH' meets all requirements:
# -e: Selects all processes.
# -f: Displays a user-oriented, full-format listing.
# -H: Displays the process hierarchy (the tree structure).
ps -efH
